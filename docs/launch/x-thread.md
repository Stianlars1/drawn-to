# Launch copy — X thread + Show HN (drafts, edit freely)

## X — thread (post the A/B composite as the image on tweet 1)

1/ Same model. Same brief. One variable.
Left: a capable coding agent with a well-engineered design skill — and an empty knowledge layer.
Right: the same model with a taste library of 51 references reverse-engineered frame by frame.
The difference is not the model. It's the library.

2/ I built Drawn To because "make it look good" is not a prompt — it's an abdication.
The agent fills the gap with its priors: soft tiles, system blue, icon + paragraph cards. Competent. Anonymous.

3/ So I reverse-engineered the things I was actually drawn to — 51 saved posts, 2 662 video frames — into 12 constants (held across the whole corpus, never asked about) and 8 style families with measured grounds, separation physics, radii, motion registers.

4/ Then a short interview in plain language. You answer with weights — "70% Linear-ish dividers + 30% Stripe-ish air" — never a forced single pick. Every answer becomes a row in a lock file. Revisions never erase; the ledger is history.

5/ It reads your repo first: tokens are adopted, the current look is a question, never an assumption. Then it builds — and every visual change must name the lock it serves.

6/ Beyond direction: an illustration engine (2–4 concepts per feature, by the feature's verb), the scroll-scrubbed product-scene pattern, and an animation doctrine distilled from @emilkowalski's skills + Apple's fluid-interface physics.

7/ It ships one person's taste. The pipeline is the point — bring your own references and the interview asks about YOUR families. Validator + CI keep every citation honest.

8/ Install: npx skills add Stianlars1/drawn-to — Claude Code, Codex, Cursor, Copilot, Gemini.
Repo + the full library: github.com/Stianlars1/drawn-to

9/ Credit where it lives: the library stands on work by @basit_designs @marcelkargul @adriankuleszo @0xSero @LexnLin @cabralorenzo @flohoeller @mnowakdesign @yurygok @ImranUxi and 23 more — every entry links to the original. If you're referenced and want anything changed: open an issue.

## Show HN

Title: Show HN: Drawn To – a taste library and lock-in interview for coding agents

Body:
I kept watching agents produce competent, anonymous UI. The fix wasn't a better prompt; it was knowledge. I reverse-engineered 51 design references frame by frame into measured constants and style families, then wrote a skill that interviews you (weights, not forced picks), writes a lock file, and builds against it. Same model + same brief, with and without the library, is the first image in the README. The pipeline (fetch → frame analysis → synthesis → validator) is reusable — swap in your own references. MIT; referenced designs are linked, not redistributed.

## One-liners for lists / directories

- awesome lists: "Drawn To — measured design-taste library + weighted lock-in interview for coding agents (51 reverse-engineered references, 12 constants, 8 families, lock files, illustration engine, animation doctrine). MIT."
